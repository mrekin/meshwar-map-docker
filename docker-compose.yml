services:
  meshwar-map:
    build: .
    container_name: meshwar-map
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    volumes:
      - ./data:/app/data
      - ./tools:/app/tools
      - ./imports:/app/imports
    env_file:
      - .env
    restart: unless-stopped
